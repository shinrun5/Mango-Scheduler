{
  "name": "fruitcrew",
  "private": true,
  "description": "Orchestrator for the Railway API service — builds the frontend, installs the backend, runs migrations, then starts the API (which also serves the built frontend). Frontend/ and Backend/ have their own package.json.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm --prefix Frontend ci && npm --prefix Frontend run build && npm --prefix Backend ci && npm --prefix Backend run migrate:deploy",
    "start": "npm --prefix Backend start"
  }
}
